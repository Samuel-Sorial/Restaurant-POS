<%- include('../includes/navbar.ejs') %>
<link rel="stylesheet" href="/css/cashier.css">

</head>
<body>
    <div class="d-flex flex-column" id="content-wrapper">
        <div class="container">
            <form id="edit-category" method="POST" action="/admin/manage-discount/edit">
                <h3 style="padding-top: 25px;">Edit an existing category discount:</h1>
                    <div class="form-group"><label>Category</label><select name="categoryId" class="form-control">
                        <optgroup label="Please select category">
                          <% if (categories.length > 0) {
                            for(let category of categories){ 
                              if(!category.CategoryDiscountDiscountId){%>
                                <option value="<%= category.categoryId %>" data-ratio="<%= category.discount %>" selected=""><%= category.name %></option>
                        <% } } }%>
                        </optgroup></select></div>
                    <div class="form-group"><label>Discount ratio:</label><input autocomplete="off" name="ratio" class="form-control ratio" aria-describedby="ratioCheck" type="number" min="0" max="100" step="1"></div>
                    <small id="ratioCheck" class="form-text text-muted">
                        If you type 30 this means you need a 30% discount. 
                    </small>
                    <br>

                    <button class="btn btn-primary btn-block text-white btn-user" type="submit">Edit Discount</button>
            </form><br>
            <form action="/admin/manage-discount/edit" method="GET"> <button id="deleteCategoryDiscount" class="btn btn-primary btn-block text-white btn-user" type="submit">Delete Discount</button></form>
            <form id="editProductDiscount" method="POST" action="/admin/manage-discount/edit">
                <h3 style="padding-top: 25px;">Edit an existing product discount:</h3>
                <div class="form-group">
                    <label>Choose product/s:</label>
                    <div class="card-body row">
                    <% for(let product of products){ if(product.hidden != 1 && product.discount){ %>
                        <div data-ratio="<%= product.discount %>" data-id="<%= product.productId%>" id="<%= product.productId%>" class="col-2 product-card">
                            <h4 data-id="<%= product.productId%>" class="product-name"><%= product.name%></h4>
                            <p  data-id="<%= product.productId%>" class="product-price">$<%= product.price%></p>
                        </div>
                    <% } } %>
                    </div>
                    <input class="prodId" type="hidden" name="productId">
                    <div class="form-group"><label>Discount ratio:</label><input  autocomplete="off" name="ratio" class="form-control discountRatio" aria-describedby="ratioCheck" type="number" min="0" max="100" step="1"></div>
                    <small id="ratioCheck" class="form-text text-muted">
                        If you type 30 this means you need a 30% discount. 
                    </small>
                    <br>
                </div>
                <br>
                <button class="btn btn-primary btn-block text-white btn-user submitDiscount">Edit Discount</button>
            </form><br>
            <form action="/admin/manage-discount/edit" method="GET"><button id="deleteProductDiscount" class="btn btn-primary btn-block text-white btn-user" type="submit">Delete Discount</button></form>
        </div><br><br>
        </div>
    <script src="/js/lib/jquery.min.js"></script>
    <script src="/js/lib/bootstrap.bundle.min.js"></script>
    <script src="/js/lib/Chart.bundle.min.js"></script>
    <script src="/js/bs-init.js"></script>
    <script src="/js/lib/jquery.easing.js"></script>
    <script src="/js/edit-discount.js"></script>
<%- include('../includes/end.ejs') %>